[project]
name = "mcs"
version = "2.0.0a0"
description = "Media Cache Server v2 — media tools and models behind an HTTP API"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
]

[project.optional-dependencies]
# The tool ops. The models' own stack (torch, transformers, insightface, faster-whisper, nemo…)
# is installed by the Containerfile, not here: those pins are architecture-specific and the
# reasons for each are written beside the RUN lines.
tools = [
  "imagehash",
  "Pillow",
  "pillow-heif",
  "rawpy",
  "PyMuPDF",
  "python-docx",
  "odfpy",
  "pytesseract",
]
test = ["pytest>=8", "httpx>=0.27"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
